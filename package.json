{
  "private": true,
  "name": "fooddeliverysystem",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace backend\" \"npm run dev --workspace frontend\"",
    "build": "npm run build --workspace frontend",
    "start": "npm run serve --workspace backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
